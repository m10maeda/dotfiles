# Mio's dotfiles

## Install

1. Download dotfiles
    ```shell
    git clone git@github.com:m10maeda/dotfiles.git ~/.dotfiles
    ```
2. Deploy dotfiles
    ```shell
    ~/.dotfiles/bin/deploy.sh
    ```
